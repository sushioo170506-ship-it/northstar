---
description: ModelResearch Step 1 — 界定调研目的、读者、决策问题、版本快照与评估维度。主编排器进入 Step 1 时读取。
globs:
alwaysApply: false
---

# ModelResearch-scope: 界定调研范围

**本 Skill 只做一件事：把"评估一个模型"这件模糊的事，收敛成一个有明确读者、明确决策问题、明确版本快照和明确评估维度的调研任务。** 不做数据采集、不写结论。

模型报告失败最常见的原因不是数据不够，而是**没想清楚给谁看、要回答什么决定**——于是写成了面面俱到的说明书。

## 输入

- 用户给的模型名 + （可选）读者、决策问题

## 输出

`model-research/output/scope.md`（模板：`templates/scope.template.md`）

---

## 一、确定读者与用途（决定严谨度与语域）

| 读者 | 关心什么 | 报告侧重 |
|------|----------|----------|
| 内部技术选型（CTO/架构/算法） | 能不能用、替代谁、迁移成本 | 能力矩阵 + 一手实测 + TCO |
| 对外交付/客户 | 可信度、可复现、风险 | 证据分级 + 复现附录 + 合规 |
| 投研 | 竞争力、护城河、趋势位置 | 纵横对标 + 生态 + 可得性 |
| 学术/技术社区 | 方法、口径、可复现 | 评测口径 + 失败模式 + 局限 |

**必须写死一句话读者画像**，后续所有取舍以它为准。

## 二、把决策问题写成可回答的形式

用户常给"调研 X 模型"，这不是决策问题。必须转成 1–3 个**可被证据回答**的问句，例如：

- "我们要不要用 X 替换现有的 Y 做 <某工作流>？"
- "X 在 <我们关心的任务> 上是否达到可上线阈值？"
- "在预算 Z 下，X vs 国内旗舰谁更合适 <场景>？"

每个决策问题标注：**判定它需要哪类证据**（基准 / 一手实测 / 成本 / 可得性）。

## 三、锁定版本与时间快照

模型迭代快，报告必须锚定：

```
模型版本/变体: [如 Mythos 5 / Fable 5，API model ID]
评估时间快照: [YYYY-MM-DD]
数据截止: [所引基准/价格/可得性信息的最新日期]
```

**声明**：此后发布的版本、基准、政策变化不在本报告范围内。

## 四、选定评估维度（4–7 个）并说明理由

从下列**模型评估维度库**中，按读者与决策问题选 4–7 个，逐个说明"为什么这个决策需要评它"。禁止全选（面面俱到=没有重点）。

| 维度 | 何时该选 |
|------|----------|
| 推理与知识 | 通用/科研/复杂问题求解 |
| 编码与工程 | 研发提效、代码 Agent |
| 长程自主 Agent | 多步任务、自动交付 |
| 多模态/视觉 | 文档/图像/图表理解 |
| 长上下文 | repo 级/长文档处理 |
| 工具使用/函数调用 | Agent 工具编排 |
| 指令遵循/结构化输出 | 工程集成可靠性 |
| 多语言/中文 | 中文业务 |
| 安全/对齐 | 高风险领域、合规 |
| 成本-效能 | 规模化、预算敏感 |
| 可得性/部署/合规 | 采购、跨境、私有化 |

## 五、明确边界（不评估什么）

显式列出**本次不评估的维度及原因**（如"不评估微调能力，因当前不涉及自训"）。边界让报告可交付，而非无限延伸。

---

## 输出格式

```markdown
# 调研范围 — [模型名]

## 读者与用途
- 读者画像: [一句话]
- 用途: [内部选型/对外交付/投研/学术]

## 决策问题
1. [问句] | 判定所需证据: [基准/实测/成本/可得性]
2. ...

## 版本与时间快照
- 模型版本/ID: 
- 评估时间: YYYY-MM-DD
- 数据截止: 

## 评估维度（4–7）
- [维度]: [为何这个决策需要它]
- ...

## 不评估（边界）
- [维度]: [原因]
```

---

## 质量卡口

- [ ] 读者画像与用途明确（一句话）
- [ ] 决策问题为可回答问句（1–3 个），且标注所需证据类型
- [ ] 版本与时间快照写死
- [ ] 选定 4–7 个维度且逐个说明理由（未全选）
- [ ] 明确列出不评估的边界
- [ ] 通篇无结论、无判断（判断属后续步骤）

通过后写：`Step 1 scope 通过 → 进入 Step 2 profile`

## 禁令

- 禁止在本步下结论或表态
- 禁止"面面俱到"式全维度铺开
- 禁止跳过版本/时间快照
