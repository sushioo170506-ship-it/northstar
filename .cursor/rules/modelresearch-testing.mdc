---
description: ModelResearch Step 3 场景化验证 Skill — 按报告类型分支做一手实测/安全红队/部署实测；含推理部署维度。主编排器进入 Step 3 时读取。
globs:
alwaysApply: false
---

# ModelResearch-testing: 场景化验证

**本 Skill 只做一件事：产出对被评模型的第一手证据，并按报告类型选择验证形态。** 这是"优质报告"与"跑分搬运"的分水岭——公开榜易过拟合/污染，只有在你自己的任务与约束下测，结论才对你有效。不下对标结论（属 write）。

## 输入

- `model-research/output/outline.md`（读者三问 → 决定验证形态；Q3 → 决定红队深度）
- `model-research/output/data.md`（可得性、API 能力、待复核的自报数据）

## 输出

`model-research/output/testing.md`（模板：`templates/testing.template.md`）

## 可用工具

- 模型 API 调用（可得时；缺凭证则提示用户在 Cursor 后台配 Secret）
- `Shell`：运行评测脚本、计时、统计

---

## 一、先判可得性（诚实高于完整）

| 情况 | 形态 |
|------|------|
| 被评模型 API 可得 | 直接实测 |
| 不可得但同底座变体可得（如 Mythos↔Fable） | 测变体，显式标"以同底座代测，Y 不可亲测" |
| 完全不可得 | 声明"无一手数据"，只做文献+口径复核，报告醒目标注该局限 |

**绝不用二手/自报数字伪装成实测。**

## 二、按报告类型分支（承接 outline 的 Q1/Q3）

| 报告类型 | 验证重点 |
|----------|----------|
| 技术报告（研究员） | **benchmark 复现**：自跑关键基准，与自报交叉，估复现成本 |
| 安全报告（监管） | **安全红队**：红线场景、越狱、有害/敏感输出、可控性；覆盖率是重点 |
| 业务/部署（PM/开发者） | **部署实测**：量化后精度损失、显存占用、推理速度、框架兼容、延迟/成本 |

## 三、必测的工程指标（骨架维度 5：推理与部署）

```
质量:    pass@1 / pass@k、评分 rubric、（安全）红线命中/漏检
延迟:    TTFT、P50/P95
吞吐/成本: 实际 token 消耗 → 每任务成本
部署:    量化支持与精度损失、显存占用、推理速度、框架兼容（vLLM/TGI/llama.cpp…）
稳定性:  重复 n 次方差、限流/超时率
失败模式: 定性记录典型错误（幻觉/不遵循/工具误用/截断/越狱成功样例）
```

## 四、记录纪律

- 每个结果附：任务、采样配置、日期、原始输出片段（或路径）
- 样本量 N 明确；小样本标"指示性，非统计显著"
- **与 data.md 的公开/自报数据交叉**：一手结果与自报是否一致？不一致是重要发现

---

## 输出格式

```markdown
# 场景化验证 — [模型名]
> 测评日期: YYYY-MM-DD · 可得性: [亲测/代测/不可得] · 验证类型: [技术/安全/部署]
> 采样配置: 温度=__ max_tokens=__ 系统提示=[…] harness=[…]

## 可得性声明
## 验证设计
| 维度/红线 | 任务数 N | 来源 | 判据 |

## 结果：质量 / 安全
| 项 | 被评 | (对照) | N | 备注 |

## 结果：推理与部署
| 指标 | 值 | 备注 |（量化损失/显存/推理速度/框架兼容/延迟/每任务成本/稳定性）

## 失败模式（定性）
## 与公开/自报数据的交叉
```

---

## 质量卡口

- [ ] 已判可得性并选定形态（亲测/代测/不可得，均须诚实声明）
- [ ] 验证类型与 outline 的读者一致（技术→复现 / 安全→红队 / 业务→部署）
- [ ] 若可得：≥1 维度有结果 + 推理部署指标 + 失败模式
- [ ] 若安全报告：红线覆盖清单 + 漏检情况
- [ ] 采样配置与样本量完整（可复现）；小样本标"指示性"
- [ ] 与 data.md 自报数据做过交叉，差异已说明

通过后写：`Step 3 testing 通过 → 进入 Step 4 process/write`

## 禁令

- 禁止用二手/自报冒充一手实测
- 禁止不记配置的"我试了感觉不错"
- 禁止把小样本说成统计显著
