---
description: Wcget Step 6 报告组装 Skill — 生成自包含 HTML 报告 + PNG 图片包。主编排器进入 Step 6 时读取本文件。
globs:
alwaysApply: false
---

# Wcget-build: 输出最终文件

将报告正文、SVG 图表和 SVG 表格组装为最终交付物。

## 输入

- `research-workflow/output/report.md`（Step 4 产物，含图表引用）
- `research-workflow/output/figures/`（Step 5 产物，所有 SVG）

## 可调用工具（可选/可替换）

> 原文使用 `build_html.py`（组装 HTML）与 `svg2png.py`（resvg-py，SVG→PNG）。在 Cursor 环境若无这些脚本，可由 Agent 直接编写等价 Python（如用 `cairosvg` / `resvg` / Playwright 截图完成 PNG 导出）；核心是满足下方样式规范与质量自检。

## 执行

### 步骤 1：生成 HTML

使用 `build_html.py` 或等价脚本构建**自包含** HTML。

**样式规范**：
- 底色：`#f5f6fa`
- 标题区：居中 + 标签药丸 + 蓝色副标题 + 浅色下划线
- 核心论点亮卡：深蓝渐变 `#0b1120→#162240` + 辉光圈 + 底部指标栏
- 章节头：蓝色编号徽章 + h2 标题 + 可选副标题
- 表格容器：无背景透明 + 表格自带白色底
- 图表容器：深色背景 `#0b1120`
- 标注框：左边框彩色 + 淡色底（蓝/绿/橙/紫四色变体）
- 过渡桥：居中蓝色 `——文字——`
- 引用：居中蓝色渐变引用块
- 结论：深色渐变 + 编号圆圈

**表格逻辑**：
- 图表 SVG → `.fg` 容器（深色背景）
- 表格 SVG → `.tfg` 容器（无背景，浅色阴影）
- 图/表编号自动识别（`fig` 前缀 → 图, `table_` 前缀 → 表）

**技术规范**：
- 响应式：640px 断点，字体缩小
- 自包含：所有 CSS 内联，所有 SVG 内联
- 打印：专色保持，表格分页控制
- 字体：Noto Sans SC / PingFang SC / sans-serif 栈

### 步骤 2：生成 PNG

将所有 SVG 转换为 PNG：

- 工具：`svg2png.py`（resvg-py）或等价（cairosvg / resvg / Playwright）
- 超采样：2×
- 中文字体：SimHei（自动注册）
- 输出目录：`research-workflow/output/figures_png/`

## 输出

```
research-workflow/output/
├── report.html                # 自包含 HTML（内联所有 SVG）
├── report.md                  # 报告正文
├── figures/                   # SVG 矢量图
│   ├── fig1_* ～ figN_*.svg
│   └── table_01 ～ table_NN.svg
└── figures_png/               # PNG 位图 (2×)
    └── *.png
```

## 质量自检（交付前终检由主编排器 wcget.mdc 统一执行）

- [ ] HTML 标签平衡（`<table>`, `<figure>`, `<section>` 开闭匹配）
- [ ] 所有 SVG 内联于 HTML
- [ ] 响应式 + 打印样式
- [ ] PNG 全部转换成功（N/N）
- [ ] 中文字体正常渲染
