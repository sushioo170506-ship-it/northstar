---
description: Wcget Step 3 数据处理/评分 Skill — 数据锚点法：维度设计→赋分→边界声明。主编排器进入 Step 3 时读取本文件。
globs:
alwaysApply: false
---

# Wcget-process: 评分方法论

**本 Skill 只做一件事：用数据给产品/方案/企业打分。** 不涉及报告分几章、每章写什么、怎么排版——那些属于 Wcget-outline 和 Wcget-write 的职责。

## 输入

- `research-workflow/output/research_data.md`（§1 产业数据 + §3 实景评价 + §2 论文数据）

## 输出

追加写入 `research-workflow/output/research_data.md`：评分矩阵追加到 §1.2（竞争格局）之后，评分依据追加到 §4（交叉验证矩阵）之后。不新建节号。

---

## 一、评分维度设计规则

### 1.1 维度数量

| 规则 | 原理 |
|------|------|
| 4–6 个维度 | 少于 4 个区分度不够；超过 6 个读者无法形成认知地图 |
| 每个维度可独立测量 | 禁止"综合实力""整体水平"等无法直接锚定数据的模糊维度 |
| 至少一个"硬参数"维度 | 可量化的性能指标（算力/速度/容量/精度/良率） |
| 至少一个"软实力"维度 | 生态/成熟度/认证/开发者规模/供应链——往往是权重最高的维度 |
| 至少一个"真实场景"维度 | 来自实景数据（用户评价/第三方评测/可靠性/召回），非纸面参数 |

### 1.2 权重设计

权重总和 = 100%。权重最高的维度必须是"软实力"或"真实场景"——因为硬参数可以靠一代产品追平，生态和信任只能用时间积累。

### 1.3 评分基准：三级数据锚点

每个评分维度必须绑定三个数据锚点，将主观打分转化为客观映射：

```
10 分锚点（天花板）: 该维度当前已知的最好水平
  → 来自学术论文的 SOTA 数据（research_data.md §2）或产业数据中最高记录（§1）
  → 只有实证数据达到或超过此锚点的产品才能得满分 10.0

5 分锚点（中位线）: 该维度的行业平均水平
  → 来自产业数据的中位数或均值（research_data.md §1）
  → 超过此锚点但未达天花板 → 5.1–9.9 分

0 分锚点（地板线）: 不具备该能力或数据为零
  → 得分为 0 或在 0.1–4.9 分之间
```

**锚点绑定格式**：
```
维度 X: [名称]（权重 __%）
  测量指标: [指标名 + 单位]
  10 分锚点: [具体数值]（来源: research_data.md §X.Y）
  5 分锚点: [具体数值]（来源: research_data.md §X.Y）
  0 分锚点: "不具备该能力" 或 [具体值 ≤ 0]
```

---

## 二、评分执行流程

### 步骤 1：确定参赛选手（5–10 个）

从 `research_data.md §1.2` 提取。必须覆盖三个层级，否则评分缺乏参考系：

| 层级 | 作用 |
|------|------|
| 行业标杆（≥1） | 提供最高参考系——其他选手的得分是相对于它计算的 |
| 中坚力量（≥3） | 构成竞争主体——该层级的得分差异揭示竞争焦点 |
| 追赶者（≥1） | 提供最低参考系——标注差距大小 |

排除规则：某个选手在 ≥ 2 个维度上数据缺失 → 降级为"不参与评分，仅文本提及"。

### 步骤 2：逐产品 × 逐维度赋分

**禁止主观估计。打分是数据映射，不是"我觉得"。**

#### 2.1 计分公式

```
得分 = 5 + 5 × (实测值 - 5 分锚点值) / (10 分锚点值 - 5 分锚点值)

约束: 最小 0.0，最大 10.0。
      5 分锚点的实测值 → 恰好 5.0 分。
      实测值超过 10 分锚点 → 仍为 10.0（满分不溢出）。
      "不具备" → 0.0。
```

#### 2.2 打分记录格式

每个得分附带计算过程，不写"我觉得 7 分"：

```
[产品名] · [维度名]: X.X 分
  实测数据: [数值]（来源: research_data.md §X.Y）
  10 分锚点: [数值]（来源: §X.Y）
  5 分锚点: [数值]（来源: §X.Y）
  计算: X.X = 5 + 5 × ([实测] - [5分锚点]) / ([10分锚点] - [5分锚点])
```

#### 2.3 加权综合公式

```
综合得分 = Σ(维度 i 得分 × 权重 i)
（数据缺失的维度不参与，剩余维度权重按比例重分配）
```

### 步骤 3：缺失数据与可信度调整

| 情况 | 处理规则 |
|------|---------|
| 数据完全缺失 | 标注 `—`，该维度不参与加权。产品排名后标注"数据不足，排名有偏差" |
| 仅企业自报，无第三方验证 | 得分 × 0.7（可信度折扣）。标注"企业自报，未经独立验证" |
| 预印本 / 未同行评议 | 标注 `†`。得分可参考但不可作为核心判断的唯一依据 |
| 纯学术数据，无产业转化 | 标注"实验室数据"。得分有效但应在评分解读中提醒"未经验证规模化可行性" |
| 数据来源自身声明了局限性 | 引用该局限性，在得分后标注范围（如"X.X ± Y"） |

### 步骤 4：未量产惩罚

无量产记录（`research_data.md §3` 中无实景评价）的产品：

- **"真实场景"维度得分 = min(纸面映射得分 × 0.5, 2.0)**
- 产品名后标注 `†（无量产记录）`
- 加权综合也标注 `†`

逻辑：没有装车/装机/上线的产品，实验室数据做得再漂亮，不可作为可靠性证据。"纸面参数对标 ≠ 商业竞争力对标。"

---

## 三、评分输出格式

评分结果追加到 `research-workflow/output/research_data.md §1.2（竞争格局）` 之后：

```
### 产品评分矩阵（数据锚点法）

#### 评分维度定义

| 维度（权重） | 测量指标 | 10 分锚点 | 5 分锚点 | 0 分锚点 |
|-------------|---------|----------|---------|---------|
| [维度1] (__%) | [指标+单位] | [数值]（§X.Y） | [数值]（§X.Y） | 不具备 |
| ... | ... | ... | ... | ... |

#### 产品得分

| 产品 | [维度1] | [维度2] | [维度3] | [维度4] | 综合 | 标注 |
|------|:---:|:---:|:---:|:---:|:---:|------|
| [产品A] | 7.2 | 8.5 | 10.0 | 9.1 | 8.7 | — |
| [产品B] | 9.8 | 8.0 | 7.0 | 1.8 | 7.0 | † 无量产记录 |

#### 单项得分计算依据

[产品A] · [维度1]: 7.2 分
  实测: [数值]（§X.Y）| 锚点: 10=[XX], 5=[XX]
  计算: 7.2 = 5 + 5 × ([实测] - [5分]) / ([10分] - [5分])

（每个产品 × 每个维度重复）
```

评分依据逐条追加到 `research_data.md §4（交叉验证矩阵）` 之后：

```
### 评分依据追溯表

| 产品 | 维度 | 得分 | 实测数据 | 10 分锚点来源 | 5 分锚点来源 | 调整说明 |
|------|------|:---:|---------|-------------|-------------|---------|
| [产品A] | [维度1] | 7.2 | [数值]（§X.Y） | [论文/报告]（§X.Y） | [报告]（§X.Y） | 无 |
| [产品B] | [维度4] | 1.8 | 无实景数据 | [XX]（§X.Y） | [XX]（§X.Y） | 未量产惩罚 |
```

---

## 四、边界条件与局限性声明

评分结果必须附带以下声明，否则评分构成误导：

1. **评分的时效性**：评分依据的数据截止日期为 [YYYY-MM-DD]。此后发布的论文、公告或量产事件未纳入。
2. **权重的可争议性**：维度权重是研究者的判断，不同决策场景下合理的权重分配可能不同。读者可根据自身需求调整权重并重新计算综合得分。
3. **锚点的选择偏差**：10 分锚点取自当前已知最高水平，不代表该维度的理论上限。随着技术进步，锚点值会漂移。
4. **跨品类不可比**：本评分仅适用于 [该品类/赛道] 内的横向比较。不同品类之间的得分不具有可比性。
5. **缺失数据的系统性偏差**：数据缺失通常意味着该产品/企业在透明度或成熟度上较弱——这种"缺数据"本身就是一种信号，但评分模型本身无法补偿。
6. **未量产产品的评分仅作方向性参考**：标注 `†` 的产品得分主要依赖纸面参数外推，不确定性远高于已量产产品。

---

## 质量自检（质量卡口）

**任何一条不通过，留在 Step 3 修改，禁止进入 Step 4。**

- [ ] 5–10 个产品，覆盖标杆 + 中坚 + 追赶者
- [ ] 4–6 个维度，每个维度有 10/5/0 三级数据锚点，锚点绑定到 research_data.md 节号
- [ ] 每个得分标注了实测数据、锚点数值和计算过程
- [ ] 硬参数 + 软实力 + 真实场景三类维度齐全
- [ ] 未量产产品已执行真实场景维度惩罚
- [ ] 缺失数据已标注调整方式（折扣/排除/†）
- [ ] 边界条件声明已输出（6 条）
- [ ] 评分依据追溯表已生成
- [ ] **没有出现任何"报告应该分几章"或"哪个评分对应哪个章节"的内容——那些属于 Wcget-write**
