---
description: Wcget 研究报告工作流主编排器。当用户要求按 Wcget / 研究报告工作流生成报告、或给出研究主题要求完整调研报告时启用。
globs:
alwaysApply: false
---

# Wcget: 研究报告自动生成工作流（编排器）

**一句话**：喂给我一个主题，我还你一份判断驱动的、有评分体系的、数据密集而简洁的、逻辑链显式闭合的专业研究报告。

**本规则是编排器，不是方法论容器。** 只做四件事：按正确顺序串联子规则、传递中间产物路径、执行质量卡口、不通过则打回修改。

试跑主题默认：`Mythos5 模型调研报告`（用户另给主题时以用户为准）。

---

## 产物路径约定（全程固定）

```
research-workflow/output/
├── research_data.md       # Step 2 创建，Step 3 追加评分
├── report.md              # Step 4 创建
├── report.html            # Step 6 创建
├── figures/               # Step 5 写入 *.svg
└── figures_png/           # Step 6 写入 *.png
```

模板目录：`research-workflow/templates/`

子规则文件（按需读取并严格遵循）：

| Step | 规则文件 | 职责 |
|------|---------|------|
| 1 | `.cursor/rules/wcget-outline.mdc` | 可争论大纲 |
| 2 | `.cursor/rules/wcget-data.mdc` | 三支柱采集 + 交叉验证 |
| 3 | `.cursor/rules/wcget-process.mdc` | 多维评分矩阵 |
| 4 | `.cursor/rules/wcget-write.mdc` | 正文撰写 |
| 5 | `.cursor/rules/wcget-figure.mdc` | SVG 图表 |
| 6 | `.cursor/rules/wcget-build.mdc` | HTML + PNG 终稿 |

---

## 执行流程（严格顺序，禁止跳步）

```
Step 1 outline → Step 2 data → Step 3 process → Step 4 write → Step 5 figure → Step 6 build → 终检
```

### 每一步的标准动作

1. **读取**对应子规则全文（`wcget-*.mdc`），按其中方法论执行。
2. **执行**该 Step，写入约定产物（或在对话中输出大纲）。
3. **对照**该 Step 的质量卡口逐条自检，在回复中列出通过/未通过项。
4. **通过** → 明确告知用户「Step N 通过，进入 Step N+1」，再继续。
5. **不通过** → 停留在本 Step 修改，禁止带着缺口进入下一步。

### 与用户的交互节奏（完整版默认）

- 每个 Step 完成后**暂停**，用简短摘要展示关键产出，询问是否继续下一步。
- 用户说「继续 / 下一步 / 通过」后再进入下一步。
- 用户说「修改 xxx」则留在本 Step 修订后再检。
- 若用户明确要求「一口气跑完」，可连续执行，但仍须逐步过卡口并在日志中记录。

---

## 各 Step 质量卡口（编排器侧速查）

### Step 1 — 大纲
- 核心结论可争论、可证伪、可行动（非描述性新闻标题）
- §1–§4 各有一句话章节结论 + ≥1 锚点
- 有头门 / 中间门 / 尾门 + 数据需求清单 + ≥3 刺点
- 通过 `wcget-outline` 自检清单全部 5 条

### Step 2 — 数据
- ≥20 产业数据点 / ≥5 篇论文 / ≥3 条实景
- ≥80% 采信数据为 A+/A/B 级
- 大纲每个锚点有对应数据；≥3 条缺口已标注
- 产出 `research-workflow/output/research_data.md`

### Step 3 — 评分
- 5–10 个选手（标杆+中坚+追赶者）
- 4–6 个维度（硬参数+软实力+真实场景），权重明确
- 每个得分有计算过程；未量产已惩罚；有边界声明
- 评分矩阵追加写入 `research_data.md`

### Step 4 — 正文
- 2500–4000 字；开篇即反常识数字或认知冲突
- 每个关键数字含「数字+时间+来源」；无禁止套话
- 产出 `research-workflow/output/report.md`

### Step 5 — 图表
- 6–10 张图；标题为学术结论句；色盲友好
- 至少 1 张多维对比 + 1 张结构分化 + 1 张时间线
- 产出 `research-workflow/output/figures/*.svg`

### Step 6 — 组装
- 自包含 HTML（SVG 内联）+ 响应式/打印样式
- PNG 全部转换成功
- 产出 `report.html` + `figures_png/`

---

## 交付前终检清单（Step 6 后必做）

### 判断驱动
- [ ] 全文 ≥ 5 个有证据支撑的判断
- [ ] 每个核心数据段结尾有「所以呢」收束
- [ ] ≥ 3 个竞争性假说被明确检验
- [ ] 核心指标做过结构分化拆解
- [ ] 评分数字之间的「断裂」被揭示

### 评分体系
- [ ] 维度 ≥ 4，权重明确
- [ ] 含「生态/成熟度」与「真实场景」维度
- [ ] 评分结果有可视化（雷达或分组柱状）

### 学术严谨性
- [ ] 研究问题可证伪
- [ ] 每个核心判断经 ≥ 2 个独立来源验证
- [ ] 不一致数据已降级或标注
- [ ] 局限性独立成节

### 写作质量
- [ ] 无「随着…发展」「有望」「前景广阔」等套话
- [ ] 一段一个判断，段首给结论
- [ ] 核心结论句 ≤ 25 字
- [ ] 标题含具体数值 + 核心判断

### 外发质量
- [ ] 引用格式规范；图表含数据来源
- [ ] HTML 响应式 + 自包含；PNG 可渲染

---

## 启动话术（用户可复制）

```
按 Wcget 工作流，生成主题「Mythos5 模型调研报告」的完整研究报告。
从 Step 1 开始，逐步执行，每步通过质量卡口后再进入下一步。
```

## 禁令

- 禁止跳过质量卡口硬进下一步
- 禁止在编排器里重写大纲/数据/写作方法论（去读对应子规则）
- 禁止用对话记忆代替文件产物；可落盘的必须写入 `research-workflow/output/`
- 禁止在数据不足时编造数字；缺口必须标注
