---
description: Wcget 研究报告工作流主 Skill（编排器）。用户说「Wcget / 研究报告工作流 / 按研究报告流程生成」或给出主题要求完整调研报告时启用。
globs:
alwaysApply: false
---

# Wcget: 研究报告自动生成工作流

**一句话解释**：喂给我一个主题，我还你一份判断驱动的、有评分体系的、数据密集而简洁的、逻辑链显式闭合的专业研究报告。

---

## 角色定位

**本 Skill 是编排器（Orchestrator），不是方法论容器。**

它只做四件事：

1. 按正确顺序串联子 Skill
2. 传递中间产物路径
3. 执行质量卡口
4. 不通过则打回该 Step 修改

怎么写大纲、怎么采集数据、怎么打分、怎么画图、怎么写正文、怎么组装终稿——**全部由对应子 Skill 负责**。编排器禁止把子 Skill 的方法论抄进本文件或临场发挥替代子 Skill。

---

## 子 Skill 职责边界

| Step | Skill 文件 | 一句话职责 | 输入 | 输出 |
|------|------------|----------|------|------|
| 1 | `wcget-outline.mdc` | 产出可争论的大纲（定刺→拆骨→填肉→设门→埋钩） | 用户主题 | 结构化大纲（对话输出；建议同步写入 `output/outline.md`） |
| 2 | `wcget-data.mdc` | 三支柱并行采集 + 三角交叉验证 | 大纲的数据需求清单 | `output/research_data.md` |
| 3 | `wcget-process.mdc` | 数据锚点法评分（维度→赋分→边界声明） | `output/research_data.md` | 评分矩阵追加写入 `research_data.md` |
| 4 | `wcget-write.mdc` | 按大纲骨架 + 数据锚点撰写正文 | 大纲 + `research_data.md` | `output/report.md` |
| 5 | `wcget-figure.mdc` | 数据→图表类型→SVG→风格收束 | `research_data.md` + 大纲锚点 | `output/figures/*.svg` |
| 6 | `wcget-build.mdc` | 组装 HTML 终稿 + PNG 导出 | `report.md` + `figures/` | `output/report.html` + `figures_png/` |

路径均相对于仓库内 `research-workflow/`。规则文件位于 `.cursor/rules/`。

---

## 执行流程（严格顺序）

```
Step 1: Wcget-outline  →  可争论的大纲（含数据需求清单 + 锚点标注）
        │
        ▼
Step 2: Wcget-data     →  research_data.md（结构化数据 + 交叉验证 + 缺口清单）
        │
        ▼
Step 3: Wcget-process  →  评分矩阵追加写入 research_data.md
        │
        ▼
Step 4: Wcget-write    →  report.md（完整报告正文）
        │
        ▼
Step 5: Wcget-figure   →  figures/*.svg（图表 + 表格 SVG）
        │
        ▼
Step 6: Wcget-build    →  report.html + figures_png/*.png
        │
        ▼
        交付前终检清单
```

### 每一步的标准动作

1. **读取**对应 `.cursor/rules/wcget-*.mdc` 全文；若仍为「占位」状态，停止并告知用户：该子 Skill 尚未完善，不能执行本 Step。
2. **执行**该子 Skill 的方法论；中间产物写入约定路径。
3. **对照**该 Step 质量卡口逐条自检，在回复中列出通过/未通过。
4. **通过** → 摘要关键产出，默认暂停，等用户确认后再进下一步（用户要求「一口气跑完」除外）。
5. **不通过** → 留在本 Step 修改，禁止跳步。

---

## 各 Step 质量卡口（编排器速查）

### Step 1 — 大纲
- 核心结论可争论、可证伪、可行动（禁止描述性新闻标题）
- §1–§4 各有一句话章节结论 + ≥1 锚点
- 头门 / 中间门 / 尾门 + 数据需求清单 + ≥3 刺点
- 通过 outline 自检清单全部 5 条

### Step 2 — 数据
- ≥20 产业数据点 / ≥5 篇论文 / ≥3 条实景
- ≥80% 采信数据为 A+/A/B 级
- 大纲每个锚点有对应数据；≥3 条缺口已标注
- 产出 `research-workflow/output/research_data.md`

### Step 3 — 评分
- 5–10 个选手（标杆+中坚+追赶者）
- 4–6 个维度（硬参数+软实力+真实场景），权重明确
- 每个得分有计算过程；未量产已惩罚；有边界声明
- 评分矩阵追加写入 `research_data.md`

### Step 4 — 正文
- 2500–4000 字；开篇即反常识数字或认知冲突
- 每个关键数字含「数字+时间+来源」；无禁止套话
- 产出 `research-workflow/output/report.md`

### Step 5 — 图表
- 6–10 张图；标题为学术结论句；色盲友好
- 至少 1 张多维对比 + 1 张结构分化 + 1 张时间线
- 产出 `research-workflow/output/figures/*.svg`

### Step 6 — 组装
- 自包含 HTML（SVG 内联）+ 响应式/打印样式
- PNG 全部转换成功
- 产出 `report.html` + `figures_png/`

---

## 最终交付物

```
research-workflow/output/
├── outline.md               # 建议保留的大纲快照
├── report.html              # 自包含 HTML（内联所有 SVG）
├── report.md                # 完整报告正文
├── research_data.md         # 结构化调研数据 + 评分矩阵
├── figures/                 # SVG 矢量图
└── figures_png/             # PNG 位图
```

---

## 交付前终检清单（Step 6 后必做）

### 判断驱动
- [ ] 全文 ≥ 5 个有证据支撑的判断
- [ ] 每个核心数据段结尾有「所以呢」收束
- [ ] ≥ 3 个竞争性假说被明确检验
- [ ] 核心指标做过结构分化拆解
- [ ] 评分数字之间的「断裂」被揭示

### 评分体系
- [ ] 维度 ≥ 4，权重明确
- [ ] 含「生态/成熟度」与「真实场景」维度
- [ ] 评分结果有可视化（雷达或分组柱状）

### 学术严谨性
- [ ] 研究问题可证伪
- [ ] 每个核心判断经 ≥ 2 个独立来源验证
- [ ] 不一致数据已降级或标注
- [ ] 局限性独立成节

### 写作质量
- [ ] 无「随着…发展」「有望」「前景广阔」等套话
- [ ] 一段一个判断，段首给结论
- [ ] 核心结论句 ≤ 25 字
- [ ] 标题含具体数值 + 核心判断

### 外发质量
- [ ] 引用格式规范；图表含数据来源
- [ ] HTML 响应式 + 自包含；PNG 可渲染

---

## 使用方式

用户输入任意主题，按 Step 1→6 顺序执行。每个 Step 完成后检查对应质量卡口，通过则进入下一步，不通过则回到该 Step 的修改循环。

**启动话术（可复制）**：

```
按 Wcget 主 Skill 编排，主题：「……」。
从 Step 1 起逐步执行；子 Skill 未完善则停止并提示。
```

默认试跑主题（若用户未改）：`Mythos5 模型调研报告`。

---

## 禁令

- 禁止跳过质量卡口硬进下一步
- 禁止在编排器内重写各子 Skill 方法论
- 禁止用对话记忆代替可落盘产物
- 禁止在数据不足时编造数字；缺口必须标注
- 禁止在子 Skill 仍为占位时假装已执行该 Step
