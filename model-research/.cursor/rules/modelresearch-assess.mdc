---
description: ModelResearch Step 5 — 综合评估：纵横对标（分维度）、安全/合规/可得性、TCO、场景适配矩阵。主编排器进入 Step 5 时读取。
globs:
alwaysApply: false
---

# ModelResearch-assess: 综合评估

**本 Skill 只做一件事：把 profile / eval / handson 的证据，整合成可支撑决策的评估。** 包括纵横对标、安全合规可得性、成本-效能、场景适配。**结论从证据浮现，不预设立场。** 不写正文（属 Step 6）。

## 输入

- `model-research/output/model_card.md` / `eval_data.md` / `handson.md`

## 输出

`model-research/output/assessment.md`（模板：`templates/assessment.template.md`）

---

## 一、纵横对标（分维度，禁止单一总分掩盖结构）

- **纵向**：与前代/同家族变体比（如 Mythos 5 vs Mythos Preview vs Opus）——看"进步在哪、退步在哪"
- **横向**：与同代竞品比——**口径能对齐才比**，不能对齐的分开列并注明
- **呈现方式**：按维度逐项对比表；**若给综合评分，必须同时展示分维度得分，并显式指出维度间的"断裂/取舍"**——不允许只报一个加权总分

### 可选：数据锚点评分（沿用 Wcget-process 方法，但不做传播）
若决策需要量化排序，可用数据锚点法（10/5/0 锚点 + 加权），但：
- 权重必须显式、可争议、可让读者自调
- 每分附计算与来源
- 附边界声明（时效/口径/缺失偏差）

## 二、安全 / 对齐（独立成节）

- 幻觉与事实性、指令遵循、拒答与过度拒答
- 越狱鲁棒性、有害输出（引用红队结论，标来源）
- 对齐机制（护栏、分类器、降级路由等）——**客观描述机制，不褒贬**

## 三、合规 / 数据治理 / 可得性（独立成节，企业向关键）

- 数据留存/训练使用/隐私政策
- 可得区域、准入门槛、许可限制
- 地缘/供应/出口管制等**可用性风险**——陈述事实与影响，标注证据强度（含"存疑"项如实标注）

## 四、成本-效能（TCO，工程测算非爆点）

```
每任务成本 = (输入token×输入单价 + 输出token×输出单价) × 每任务调用次数
综合还要看: token 效率（完成同任务的实际消耗）、延迟、限流、缓存/批处理折扣
```

- 用 handson 的实测 token 消耗（无则用估算并标注）
- 给"每典型任务成本"而非只列标价
- 口径不一的价格取区间并标注（承接 eval/profile 的分级）

## 五、场景适配矩阵（回指 Step 1 决策问题）

对 Step 1 的每个决策问题 / 每类目标场景，给出：

```
| 场景 | 推荐/条件推荐/不推荐 | 支撑证据(指向 eval/handson 节) | 迁移成本 | 否决条件 |
```

**结论必须可回指证据**，不能是印象。

---

## 输出格式

```markdown
# 综合评估 — [模型名]
> 评估日期: YYYY-MM-DD

## 1. 纵横对标（分维度）
| 维度 | 被评模型 | 前代 | 竞品A | 竞品B | 口径一致? | 结论(该维度) |
|------|:---:|:---:|:---:|:---:|:---:|------|
（如做综合评分，另附评分表 + 权重 + 维度断裂说明）

## 2. 安全/对齐
- [机制描述 + 红队/越狱证据 + 来源]

## 3. 合规/数据治理/可得性
- [留存/许可/区域/地缘风险 + 证据强度]

## 4. 成本-效能（TCO）
| 模型 | 输入/输出单价 | 每任务token(实测/估) | 每任务成本 | 延迟 | 备注 |
|------|------|------|------|------|------|

## 5. 场景适配矩阵
| 场景/决策问题 | 结论 | 支撑证据 | 迁移成本 | 否决条件 |
|------|------|----------|----------|----------|

## 关键取舍（Trade-offs）
- [该模型的核心取舍，中立陈述]
```

---

## 质量卡口

- [ ] 纵向 + 横向对标均**分维度**呈现；若有总分则同时展示分项并指出断裂
- [ ] 横向对比只在口径可对齐处比大小，其余标注不可比
- [ ] 安全/对齐、合规/可得性 各自独立成节
- [ ] TCO 给"每任务成本"，用实测或标注估算
- [ ] 场景适配矩阵每条结论可回指 eval/handson 证据
- [ ] 通篇中立，关键取舍双向陈述（有利/不利都写）

通过后写：`Step 5 assess 通过 → 进入 Step 6 write`

## 禁令

- 禁止用单一加权总分代替分维度分析
- 禁止无证据回指的印象式结论
- 禁止对安全/可得性事实做情绪化渲染（含"存疑"项须如实标注）
