---
description: ModelResearch Step 7 — 图表 + 多格式组装：自包含 HTML/PNG，按需 Word（公文/通用）与飞书文档。主编排器进入 Step 7 时读取。
globs:
alwaysApply: false
---

# ModelResearch-build: 图表与多格式组装

**本 Skill 做两件事：为评估报告生成客观图表；把 report.md 组装为多格式交付物。** 图表服务于"看清证据"，不服务于"制造爆点"。

## 输入

- `model-research/output/report.md`（Step 6 产物，含图表引用）
- `model-research/output/eval_data.md` / `assessment.md`（图表数据源）

## 输出

```
model-research/output/
├── figures/*.svg          # 图表 + 表格
├── figures_png/*.png      # 2× PNG
├── report.html            # 自包含 HTML（SVG 内联）
├── report.docx            # Word 通用
├── report.gongwen.docx    # Word 公文（GB/T 9704-2012）
└── report.feishu.md       # 飞书可导入
```

## 输出格式选择（执行前确认）

默认 `html`+`png`；可加 `docx`（选 `gongwen`/`general` 模板）、`feishu`。
（组装/转换的样式规范、工具、脚本与 Wcget-build 一致，可直接复用 `research-workflow/scripts/` 下的 `build_report.py` / `svg_to_png.py` / `build_docx.py` / `build_feishu.py`，仅替换输入输出目录为 `model-research/output/`。）

---

## 图表规范（评估报告专用差异）

沿用通用作图纪律（色盲友好、无 3D、无饼图、仅水平网格、SVG 文字可编辑、标数据来源），**但**：

- **标题为客观描述句，不是结论爆点。**
  - 好：`各模型 SWE-Bench Pro 得分（厂商自报，xhigh，2026-06）`
  - 不好：`Mythos 领先 22 分，但国内已挤入同一梯队`（这是 Wcget 的观点式标题，评估报告不用）
- **图上必须标注口径与证据强度**（如在标题/副标题/脚注注明"厂商自报""第三方""自测"）。
- 混排不可比数据的图要么不画，要么分面（facet）并注明。

### 模型报告常用图

| 图 | 类型 | 用途 |
|----|------|------|
| 分维度基准对比 | 分组柱状/横向条形 | 各维度谁高谁低（同口径） |
| 能力雷达 | 雷达图 | 多维能力轮廓（标注权重来源） |
| 纵向演进 | 折线/斜率图 | 与前代对比 |
| 成本-能力 | 散点/象限 | TCO 与能力的权衡 |
| 延迟/成本实测 | 柱状/箱线 | 一手工程指标 |
| 评分矩阵 / TCO 表 / 场景适配表 | 表格 SVG | 结构化呈现 |

图件总数 6–10（含表格）。至少含：1 张分维度能力对比 + 1 张成本/工程指标 + 1 张场景适配或评分表。

---

## Word 模板

- `general`：通用研报格式（雅黑标题、宋体正文 1.5 倍行距、图题含口径与来源、可选封面）
- `gongwen`：党政机关公文格式（GB/T 9704-2012：方正小标宋标题、仿宋三号正文、页边距 37/35/28/26mm、页码规范、三线表）

Word/飞书无法内联 SVG → 复用 `figures_png/*.png`（≥300 DPI）；飞书评分/适配表用原生表格块，并保留"待跟进问题"块。

---

## 质量卡口（交付前终检由主编排器统一执行）

- [ ] 用户选定的每种格式均已产出
- [ ] 图表标题为客观描述句，标注口径与证据强度
- [ ] 不可比数据未混在同一张对比图（或已分面注明）
- [ ] HTML 自包含（SVG 内联）+ 响应式/打印
- [ ] PNG 全部转换成功，供 docx/feishu 复用
- [ ] Word 模板正确（gongwen 字体/页边距/页码；general 图题含来源）
- [ ] 飞书保留原生表格 + 待跟进块
- [ ] 图表复现信息（数据源/脚本）可追溯

通过后进入交付前终检（见 `modelresearch.mdc`）。

## 禁令

- 禁止观点式/爆点式图表标题
- 禁止把不同口径数据混在一张图比大小
- 禁止图表无来源/无口径标注
