---
description: ModelResearch Step 3 — 能力评估框架：为每个维度绑定权威基准、标注评测口径、按自报/三方/自测分级证据。主编排器进入 Step 3 时读取。
globs:
alwaysApply: false
---

# ModelResearch-eval: 能力评估框架与证据分级

**本 Skill 只做一件事：把 Step 1 选定的评估维度，落成"维度 × 基准 × 口径 × 证据强度"的结构化能力数据。** 这是模型报告区别于二手拼凑的核心——**在评测口径上较真**。不做对标结论（属 Step 5）、不写正文。

## 输入

- `model-research/output/scope.md`（评估维度）
- `model-research/output/model_card.md`

## 输出

`model-research/output/eval_data.md`（模板：`templates/eval_data.template.md`）

## 可用工具

- `WebSearch` / `WebFetch`：官方评测披露、第三方 leaderboard、评测论文
- 学术检索：基准的原始定义论文

---

## 一、维度 → 基准映射

为 Step 1 的每个评估维度绑定 ≥1 个**权威、口径清晰**的基准。常用映射（示例，非穷举）：

| 维度 | 常用基准 |
|------|----------|
| 编码与工程 | SWE-Bench (Verified/Pro)、Terminal-Bench、FrontierCode、LiveCodeBench |
| 长程自主 Agent | OSWorld、AutomationBench、GAIA、agent swarm 类 |
| 推理与知识 | HLE、GPQA、MMLU-Pro、AIME |
| 多模态/视觉 | CharXiv、MMMU、文档理解类 |
| 长上下文 | RULER、needle-in-haystack、长文档 QA |
| 工具/函数调用 | MCPMark、BFCL、τ-bench |
| 中文 | SuperCLUE、C-Eval、CMMLU |
| 安全 | 越狱鲁棒性、有害内容、拒答率（多为定性/红队） |

**若某维度无公开权威基准** → 明确标注，转由 Step 4 一手实测补足。

## 二、评测口径（每个基准数字必须标注）——本 Skill 的灵魂

一个跑分脱离口径就是噪声。每条基准数据**必须**记录：

```
基准名 + 版本/子集     (如 SWE-Bench Pro，Diamond 子集)
评测配置              (harness、effort/推理档、是否带工具、agent scaffold)
采样口径              (pass@1 / pass@k、温度、n)
测评方                (谁跑的：模型厂商 / 第三方 / 本报告自测)
日期                  (跑分日期，非引用日期)
```

### 口径不可比警告（强制执行）
- **不同 harness / effort / 是否带工具 → 不可直接比大小**
- **不同语言基准（SWE-Bench 英文 vs SuperCLUE 中文）→ 不可换算**
- **不同版本子集 → 标注差异**
- 遇到不可比，**分开列、不合算**，并写一句"不可比原因"

## 三、证据强度分级（每条数据必须标）

| 级别 | 定义 | 使用规则 |
|------|------|----------|
| **自测** | 本报告用受控条件亲测 | 最高可信，需附配置 |
| **第三方** | 独立评测机构/leaderboard，非厂商 | 可支撑判断 |
| **自报** | 模型厂商自己公布 | 可用但须标"厂商自报，缺独立复现"，不得当第三方 |
| **二手** | 媒体/博客转述 | 仅线索，必须回源升级或降级 |

> 硬规则：**厂商自报的跑分不得作为"领先/落后"结论的唯一依据**——至少要有第三方或自测交叉，或在结论里显式降级。

## 四、纵横数据一并采集（供 Step 5 对标）

同一基准，尽量同时采集：
- **被评模型**的分
- **纵向**：其前代/同家族变体的分
- **横向**：同代竞品的分（口径能对齐才采，不能对齐则标注）

---

## 输出格式

```markdown
# 能力评估数据 — [模型名]
> 采集日期: YYYY-MM-DD
> 证据分布: 自测 __ · 第三方 __ · 自报 __ · 二手 __
> 不可比对照组: __ 处已标注

## 维度 1: [名称]（对应决策问题 #）
### 绑定基准
| 基准(版本/子集) | 被评模型 | 前代 | 竞品A | 竞品B | 配置(harness/effort/工具/pass@k) | 测评方 | 日期 | 证据级 |
|-----------------|:---:|:---:|:---:|:---:|------|------|------|:---:|

### 口径与不可比说明
- [哪些数字不可直接比，为什么]

## 维度 2: ...

## 无公开基准的维度
| 维度 | 原因 | 转交 Step 4 实测项 |

## 证据分级汇总与风险
- [自报占比过高的维度 → 提示 Step 5 谨慎]
```

---

## 质量卡口

- [ ] Step 1 每个评估维度都有对应基准或"转实测"标注
- [ ] 每条基准数据标注：版本/子集 + 配置 + 采样口径 + 测评方 + 日期
- [ ] 每条数据标注证据强度（自测/三方/自报/二手）
- [ ] 不可比的数据已分开列并注明原因，未强行合算
- [ ] 厂商自报数据已显式标注，未冒充第三方
- [ ] 纵向/横向对照数据尽量齐全（缺口标注）

通过后写：`Step 3 eval 通过 → 进入 Step 4 handson`

## 禁令

- 禁止记录无口径的裸跑分
- 禁止把不同 harness/语言/子集的分混在一列比大小
- 禁止把自报当第三方
