---
description: ModelResearch Step 4 — 一手实测：私有 eval、延迟/吞吐/成本、失败模式；不可得时诚实声明并给退化方案。主编排器进入 Step 4 时读取。
globs:
alwaysApply: false
---

# ModelResearch-handson: 一手实测

**本 Skill 只做一件事：产出对被评模型的第一手证据。** 这是"优质模型报告"与"跑分搬运工"的分水岭——公开榜易被过拟合/污染，只有在**你自己的任务和约束**下测，结论才对你有效。不做对标结论（属 Step 5）。

## 输入

- `model-research/output/scope.md`（决策问题、评估维度）
- `model-research/output/model_card.md`（可得性、API 能力）
- `model-research/output/eval_data.md`（哪些维度"无公开基准，转实测"）

## 输出

`model-research/output/handson.md`（模板：`templates/handson.template.md`）

## 可用工具

- 模型 API 调用（若在本环境可得；缺凭证时提示用户在 Cursor 后台配置 Secret）
- `Shell`：运行评测脚本、计时、统计
- 私有 eval 数据集（用户提供或就 scope 决策问题现构一小组代表性任务）

---

## 一、先判定可得性（决定实测形态）

| 情况 | 实测形态 |
|------|----------|
| 被评模型 API 可得 | 直接实测（首选） |
| 不可得，但同底座变体可得（如 Mythos↔Fable） | 测可得变体，**显式标注"以同底座 X 代测，Y 不可亲测"** |
| 完全不可得 | 诚实声明"无一手数据"，只做文献+口径复核，并在报告醒目标注该局限 |

**诚实高于完整**：不可得就写不可得，绝不用二手数字伪装成实测。

## 二、构建私有 eval（小而代表性）

不追求大规模，追求**贴合 Step 1 决策问题**：

- 每个关键维度 5–15 个代表性任务（来自真实业务样本更佳）
- 明确评分标准（通过/失败判据、评分 rubric）
- 固定采样配置（温度、max tokens、系统提示）并记录——保证可复现

## 三、必测的工程指标（跑分之外，决策更看重）

```
正确性/质量:  pass@1 / pass@k、评分 rubric 得分
延迟:        首 token 延迟 (TTFT)、总时延 (P50/P95)
吞吐/成本:    实际消耗 token（输入+输出）→ 每任务成本
稳定性:      重复 n 次的方差、限流/超时率
失败模式:    定性记录典型错误（幻觉/不遵循指令/工具误用/长输出截断）
```

> 失败模式的定性记录，往往比跑分更能指导"能不能上线"。

## 四、记录纪律

- 每个结果附：任务、配置、日期、原始输出片段（或链接/路径）
- 样本量 N 明确标注；小样本结论标"指示性，非统计显著"
- 与 Step 3 的公开基准**交叉**：一手结果与自报是否一致？不一致是重要发现

---

## 输出格式

```markdown
# 一手实测 — [模型名]
> 测评日期: YYYY-MM-DD · 可得性: [亲测/代测/不可得]
> 采样配置: 温度=__ max_tokens=__ 系统提示=[…] harness=[…]

## 可得性声明
- [被评模型是否可亲测；不可得的退化方案]

## 私有 eval 设计
| 维度 | 任务数 N | 任务来源 | 评分判据 |
|------|:--:|------|----------|

## 结果：质量
| 维度 | 指标 | 被评模型 | (对照可得竞品) | 样本量 | 备注 |
|------|------|:---:|:---:|:--:|------|

## 结果：工程指标
| 指标 | 值 | 备注 |
|------|----|----|
| TTFT / P50 / P95 延迟 | | |
| 每任务平均 token / 成本 | | |
| 稳定性（方差/超时率） | | |

## 失败模式（定性）
- [典型错误 1 + 复现条件 + 原始片段]

## 与公开基准的交叉
- [一手结果 vs Step 3 自报数据是否一致；差异说明]
```

---

## 质量卡口

- [ ] 已判定可得性并选定实测形态（亲测/代测/不可得均可，但须诚实声明）
- [ ] 若可得：≥1 个维度有私有 eval 结果 + 延迟/成本/失败模式记录
- [ ] 若不可得：明确声明，报告醒目位置标注该局限
- [ ] 采样配置与样本量记录完整（可复现）
- [ ] 与 Step 3 公开数据做过交叉，差异已说明
- [ ] 小样本结论已标"指示性"

通过后写：`Step 4 handson 通过 → 进入 Step 5 assess`

## 禁令

- 禁止用二手/自报数字冒充一手实测
- 禁止不记配置的"我试了一下感觉不错"
- 禁止把小样本指示性结果说成统计显著
