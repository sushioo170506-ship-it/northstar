---
description: ModelResearch 模型调研报告工作流主编排器。用户要求「按 ModelResearch / 模型调研 / 评估某模型出调研报告」时启用。区别于 Wcget（公众号研究文）：本流程产出中立、可复现的技术评估文档。
globs:
alwaysApply: false
---

# ModelResearch: 模型调研报告工作流（编排器）

**一句话**：给我一个模型 + 决策问题，我还你一份中立、可复现、可追责的模型评估报告——回答"它能干什么、干不了什么、要不要用、替代谁、用在哪"。

**本规则是编排器，不是方法论容器。** 只做四件事：按顺序串联子规则、传递中间产物路径、执行质量卡口、不通过则打回。

## 与 Wcget 的分工

- 研究"一个市场/趋势/观点" → 用 `wcget.mdc`（判断驱动、传播型长文）
- 评估"一个具体模型制品" → 用**本流程**（中立评估、决策支持）

**语域纪律（全流程强制）**：中立客观、结论从证据浮现；**禁止**立场先行、传播节奏（头门/埋钩/金句）、未标口径的跑分、把"评估"写成"评论"。

---

## 产物路径约定（全程固定，位于 `model-research/output/`）

```
model-research/output/
├── scope.md          # Step 1
├── model_card.md     # Step 2
├── eval_data.md      # Step 3（Step 4/5 追加）
├── handson.md        # Step 4
├── assessment.md     # Step 5
├── report.md         # Step 6
├── report.html/.docx/.gongwen.docx/.feishu.md
├── figures/          # Step 7 *.svg
└── figures_png/      # Step 7 *.png
```

模板目录：`model-research/templates/`

## 子 Skill 职责边界

| Step | 规则文件 | 职责 | 产出 |
|------|---------|------|------|
| 1 | `modelresearch-scope.mdc` | 调研目的·读者·决策问题·版本快照·评估维度选择 | `scope.md` |
| 2 | `modelresearch-profile.mdc` | Model Card 事实基线（不带判断） | `model_card.md` |
| 3 | `modelresearch-eval.mdc` | 能力评估框架 + 基准口径 + 证据分级（自报/三方/自测） | `eval_data.md` |
| 4 | `modelresearch-handson.mdc` | 一手实测（私有 eval / 延迟 / 成本 / 失败模式）或诚实标注不可得 | `handson.md` |
| 5 | `modelresearch-assess.mdc` | 纵横对标 + 安全合规可得性 + TCO + 场景适配矩阵 | `assessment.md` |
| 6 | `modelresearch-write.mdc` | 中立评估语域正文 + 风险登记 + 复现附录 | `report.md` |
| 7 | `modelresearch-build.mdc` | 图表 + 多格式组装（HTML/PNG/Word[公文/通用]/飞书） | `report.*` |

---

## 执行流程（严格顺序，禁止跳步）

```
scope → profile → eval → handson → assess → write → build → 交付前终检
```

### 每步标准动作

1. **读取**对应 `modelresearch-*.mdc` 全文；若为占位则停止并提示"该子 Skill 未完善"。
2. **执行**方法论，产物写入约定路径。
3. **对照**该步质量卡口逐条自检，回复中列出通过/未通过。
4. **通过** → 摘要关键产出，默认暂停等用户确认（用户要求"一口气跑完"除外）。
5. **不通过** → 留在本步修改，禁止带缺口进入下一步。

---

## 各步质量卡口（编排器速查）

### Step 1 scope
- 明确读者、决策问题、评估的版本与时间快照
- 选定 4–7 个评估维度并说明为何选它们
- 明确"不评估什么"（边界）

### Step 2 profile
- Model Card 覆盖：定位/家族关系、上下文、模态、训练截止、API 能力面、定价、可得性/许可
- **纯事实、不带判断**；每条有来源

### Step 3 eval
- 每个能力维度绑定 ≥1 权威基准，并标注**口径**（版本/harness/effort/是否带工具/pass@k/温度/日期）
- 每个数字标注证据强度：自报 / 第三方 / 自测
- 不同口径的基准**不混算**；跨语言/跨版本明确"不可比"

### Step 4 handson
- 有可得模型 → 用私有 eval 实测 ≥1 维度 + 记录延迟/成本/失败模式
- 不可得 → 诚实声明，并说明退化方案（测同底座可得版/纯文献）

### Step 5 assess
- 纵向（前代）+ 横向（同代竞品）对标，**分维度呈现**，禁止用单一加权总分掩盖结构差异
- 安全/对齐 + 合规/数据治理 + 可得性/地缘风险 独立成节
- TCO：单价 × token 效率 × 延迟 × 限流 → 每任务成本
- 场景适配矩阵：哪些任务推荐/不推荐 + 证据

### Step 6 write
- 中立评估语域；每个关键数字含"数字+口径+来源+日期"
- 结论分场景（推荐/否决条件），从证据浮现
- 含风险登记表 + 局限性独立成节 + **可复现附录**（prompt/配置/来源分级）
- 无套话、无立场先行、无传播节奏钩子

### Step 7 build
- 进入前确认输出格式（html/png 默认，可加 docx[gongwen/general]/feishu）
- 图表标题为客观描述句（非结论爆点），标口径与来源
- 自包含 HTML + PNG；选定格式均产出

---

## 交付前终检清单（Step 7 后必做）

### 中立性与证据
- [ ] 无立场先行；结论由证据支撑，且给出反面/边界情形
- [ ] 每个基准数字标注口径 + 证据强度（自报/三方/自测）
- [ ] 不一致或不可比数据已标注，未强行合算
- [ ] 局限性 + 风险登记独立成节

### 模型报告完整性
- [ ] Model Card 事实基线齐全
- [ ] 能力矩阵按维度组织，纵横对标分维度呈现
- [ ] 有一手实测或明确的"不可得"声明
- [ ] TCO / 成本-效能有工程测算，非标价爆点
- [ ] 场景适配矩阵 + 分场景采纳建议 + 迁移成本

### 可复现
- [ ] 复现附录：prompt、配置、评测日期、来源清单与分级
- [ ] 版本与时间快照明确

### 外发质量
- [ ] 图表含口径与来源；HTML 自包含；选定格式全部产出

---

## 使用方式

```
按 ModelResearch 主 Skill 编排，评估模型：「<模型名>」。
读者：<内部选型/对外交付/投研/学术>；决策问题：<要不要用/替代谁/用在哪>。
从 Step 1 起逐步执行；子 Skill 未完善则停止并提示。
```

## 禁令

- 禁止立场先行 / 传播节奏 / 金句钩子（那是 Wcget 的活）
- 禁止未标口径的跑分、把自报当第三方
- 禁止用单一加权总分掩盖维度结构差异
- 禁止在编排器内重写子 Skill 方法论
- 禁止用对话记忆代替文件产物；数据不足时标注缺口，不编造
