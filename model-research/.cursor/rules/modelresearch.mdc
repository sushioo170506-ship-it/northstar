---
description: ModelResearch 模型调研报告工作流主编排器。用户要求「按 ModelResearch / 模型调研 / 评估某模型出调研报告」时启用。产出中立、可复现的技术评估文档，区别于 Wcget（公众号研究文）。
globs:
alwaysApply: false
---

# ModelResearch: 模型调研报告工作流（编排器）

**一句话**：给我一个模型 + 读者 + 决策问题，我还你一份中立、可复现、可追责、且**对得起特定读者**的模型评估报告。

**本规则是编排器，不是方法论容器。** 只做：按顺序串联子 Skill、传路径、执行质量卡口（含复核）、不通过则打回。

**一份优质模型报告 = 完整 8 维骨架（不遗漏）+ 精准主体匹配（3 问）。** 缺骨架=言之无物，缺匹配=自说自话。

## 与 Wcget 分工

- 研究"市场/趋势/观点" → `wcget.mdc`（判断驱动、传播型）
- 评估"具体模型制品" → 本流程（中立评估、决策支持）

**语域纪律（全程强制）**：中立客观、结论从证据浮现、有利不利都写；**禁止**立场先行、传播节奏（头门/埋钩/金句）、未标口径的跑分、把评估写成评论。

---

## Skill 组成（5 个专属 + 3 个复用 Wcget）

> skill 是封装好的工具；8 维骨架 / 3 问 / 7 步流程是装进这些工具里的方法论，**不是**一步一个 skill。

| Step | Skill | 处置 | 职责 | 产出 |
|------|-------|------|------|------|
| 1 | `modelresearch-outline` | 专属 | 读者三问 → 8 维加权大纲 + 结论形式 + 风险权重（灵魂） | `outline.md` |
| 2 | `modelresearch-data` | 专属 | 信息收集 + 技术解构 + 评测对照（来源分级/口径/contamination） | `data.md` |
| 3 | `modelresearch-testing` | 专属 | 场景化验证：实测/红队/部署（按报告类型分支） | `testing.md` |
| 4 | `wcget-process` | **复用** | 需量化排序时：数据锚点评分 | 评分追加 `data.md` |
| 5 | `modelresearch-write` | 专属 | 对比分析 + 主体适配撰写（金字塔/结论形式/风险登记/复现附录） | `report.md` |
| 6 | `wcget-figure` + `wcget-build` | **复用** | 客观图表 + 多格式组装（HTML/PNG/Word[公文/通用]/飞书） | `report.*` |

复用 Wcget skill 时：图表标题改**客观描述句**（非爆点）、标口径；产物写入 `model-research/output/`（脚本 `research-workflow/scripts/` 可复用，改输入输出目录）。

产物目录：`model-research/output/`；模板：`model-research/templates/`。

---

## 执行流程（严格顺序）

```
outline → data → testing → [process(可选)] → write → figure+build → 复核终检
```

每步：读取子 Skill 全文 → 执行 → 对照该步质量卡口自检并列出 → 通过则摘要+暂停待确认 → 不通过留在本步改。子 Skill 若为占位则停止提示。

---

## 各步质量卡口（速查）

- **outline**：三问已答→导出权重/结论形式/风险侧重；8 维全覆盖（边界显式）；大纲带篇幅权重
- **data**：每章证据需求有采集；Model Card+架构+训练+版本 齐全；基准带口径+证据级；查过 contamination；同量级/同口径才比
- **testing**：可得性诚实声明；验证类型与读者一致（技术复现/安全红队/部署实测）；含推理部署指标；与自报交叉
- **process（可选）**：数据锚点 10/5/0 + 权重可调 + 每分有计算 + 边界声明
- **write**：对比分析（双向）；摘要给决策答复且形式=outline Q2；四要素数字；结论可回指；风险章权重=Q3；复现附录区分官方声称/第三方
- **figure+build**：图表标题客观+标口径；不可比不混图；HTML 自包含；选定格式全产出

---

## 交付前终检清单（含"复核"Step，Step 6 后必做）

### 中立性与证据
- [ ] 无立场先行；有利/不利证据都在；结论由证据支撑
- [ ] 每个基准数字标口径 + 证据强度（自测/三方/自报/二手）
- [ ] **区分"官方声称"与"第三方复现"**；不一致/不可比已标注

### 复核（对应通用流程 Step 7）
- [ ] 数据是否标注来源和时间
- [ ] 是否有利益冲突声明（如有）
- [ ] **读者视角检查：读者读完能否据此做出其下一步决策？**

### 骨架与主体匹配
- [ ] 8 维骨架均有着落（不适用者已标边界）
- [ ] 结论形式匹配读者下一步（Yes/No / 复现成本 / 绿黄红 / 部署可行性）
- [ ] 风险篇幅匹配"读者最怕什么"

### 完整性与外发
- [ ] Model Card / 能力矩阵（分维度）/ 一手实测或不可得声明 / TCO / 适配矩阵 齐全
- [ ] 复现附录（口径/配置/来源分级/版本快照）
- [ ] 图表含口径与来源；HTML 自包含；选定格式全部产出

---

## 使用方式

```
按 ModelResearch 主 Skill 编排，评估模型：「<模型名>」。
读者：<研究员/PM/开发者/投资人/监管>；决策问题：<要不要用/替代谁/用在哪/能否传播>。
从 Step 1 起逐步执行；子 Skill 未完善则停止并提示。
```

## 禁令

- 禁止把"框架步骤"机械当成 skill 去堆砌（方法论装进现有工具）
- 禁止立场先行 / 传播节奏 / 金句钩子
- 禁止未标口径跑分、把自报当第三方
- 禁止用单一加权总分掩盖维度结构差异
- 禁止用对话记忆代替文件产物；数据不足标缺口不编造
