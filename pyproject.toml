[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "northstar-research-workflow"
version = "0.1.0"
description = "Persistent, resumable research report skill workflow"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Northstar contributors" }]
dependencies = []

[project.optional-dependencies]
office = [
    "python-docx>=1.2.0",
    "python-pptx>=1.0.2",
]

[project.scripts]
research-workflow = "research_workflow.cli:main"
research-workflow-feishu-bot = "research_workflow.feishu_bot:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
