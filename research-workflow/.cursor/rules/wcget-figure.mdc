---
description: Wcget Step 5 图表 Skill — 纯作图方法论：图表类型选择、视觉风格、表格渲染、导出标准。主编排器进入 Step 5 时读取本文件。
globs:
alwaysApply: false
---

# Wcget-figure: 图表输出方法论

**本 Skill 只做一件事：规范"怎么做图"——根据数据特征选择图表类型、统一视觉风格、设定导出标准。** "做哪些图、每张图对应什么内容"由 Wcget-outline 的大纲锚点决定，"图中用什么数据"来自 Wcget-data 产出的 research_data.md。

## 可调用工具（可选/可替换）

> 原文引用 `nature-figure`（Python matplotlib/seaborn → SVG 的绘图合约入口）与 `render_tables.py`。在 Cursor 环境若无这些脚本，可用等价方式：由 Agent 直接编写 Python（matplotlib + `svg.fonttype='none'`）生成 SVG，并自建表格渲染脚本。核心是遵守本 Skill 的类型选择逻辑与风格规范。

| 工具 | 时机 | 作用 |
|------|------|------|
| 绘图入口（如 `nature-figure`）| 每张图生成时 | 执行绘图合约（结论→证据链→面板映射→配色→导出）|
| 子 Agent 并行 | 图 ≥ 6 张时（推荐） | 每张图分派独立 Agent 并行生成，最后统一风格收束 |
| Shell 脚本 | 表格 SVG 渲染时 | 执行 `render_tables.py` 或等价脚本 |

**调用纪律**：绘图入口是图表生成的唯一入口——不绕过它直接乱调 matplotlib；并行生成的图必须最后统一风格收束（配色/字体/字号/线宽对齐）；绘图合约（核心结论→证据链→面板映射→审稿风险）每张图生成前必须填完。

## 输入

- `research-workflow/output/research_data.md`（评分矩阵 + 产业数据 + 实景数据 + 论文数据）
- 大纲中的锚点标注（哪些数据锚点需要可视化）

## 输出

```
research-workflow/output/figures/
├── fig1_*.svg ～ figN_*.svg    # N 张图表 SVG
└── table_01.svg ～ table_NN.svg # N 张表格 SVG
```

---

## 一、图表类型选择逻辑

图表类型不由"大纲说 figN 应该是什么类型"决定，而由**数据本身的特征**决定。

### 1.1 选择决策树

```
待可视化的数据是什么结构？
│
├─ 比较（多个对象在同一个指标上的对比）
│   ├─ 对象 ≤ 6 个，指标 ≤ 2 个 → 分组柱状图
│   ├─ 对象 ≤ 6 个，指标 ≥ 3 个 → 雷达图
│   ├─ 对象 > 6 个 → 横向条形图（按值排序）
│   └─ 需要展示排名变化 → 斜率图（slope chart）
│
├─ 趋势（同一指标随时间/序列变化）
│   ├─ 1–2 条线 → 折线图
│   ├─ 3–5 条线 → 分组折线图
│   ├─ ≥ 6 条线 → 小多组图（small multiples），每条线独立子图
│   └─ 需要展示构成变化 → 堆叠面积图
│
├─ 分布（数据在某个范围内的散布情况）
│   ├─ 单变量 → 直方图或密度曲线
│   ├─ 双变量 → 散点图
│   ├─ 三变量 → 气泡图（第三变量映射为气泡大小）
│   └─ 需要展示统计特征 → 箱线图或小提琴图
│
├─ 构成（部分与整体的关系）
│   ├─ 静态构成 → 堆叠柱状图（优于饼图）
│   ├─ 动态构成变化 → 堆叠面积图
│   └─ 层级构成 → 树图（treemap）
│
├─ 关系（两个或多个变量之间的关联）
│   ├─ 连续 × 连续 → 散点图 + 回归线
│   ├─ 连续 × 连续 × 分组 → 分面散点图
│   ├─ 矩阵关系（≥ 3 变量） → 相关性热力图
│   └─ 网络/连接关系 → 网络图或桑基图
│
└─ 空间/排序（位置或等级）
    ├─ 二维排序 → 热力图
    ├─ 一维排序 + 分组 → 点阵图（dot plot）
    └─ 两个维度的交叉评价 → 象限图（quadrant chart）
```

### 1.2 选择纪律

- **饼图禁止**：人眼不擅长比较角度。用堆叠柱状图替代
- **3D 禁止**：透视变形扭曲数据感知。3D 柱状图/3D 饼图一律不用
- **双 Y 轴谨慎**：仅在两个指标量纲完全不同且必须叠加时使用。能拆成上下对齐子图就不用双 Y 轴
- **气泡图的气泡面积映射到数值**：气泡的**面积**（不是半径）与数值成正比——这是最常见的作图错误

---

## 二、视觉风格规范

### 2.1 配色

```
原则 1: 色盲友好。使用 matplotlib 'tab10' 调色板或 Color Universal Design (CUD) 方案。
原则 2: 灰阶可区分。打印到黑白仍可区分各组——用线型/标记形状作为颜色之外的区分维度。
原则 3: 每张图主色 ≤ 3 种。超过 3 个分组时用同一色系的不同明度/饱和度，而非堆更多颜色。
原则 4: 红绿仅用于方向性信号（上涨/下跌、增益/损失、正/负）。普通分类对比不用红绿。
```

### 2.2 字体与字号

```
标题: 12pt, bold（学术结论句，非描述性标签）
轴标签: 9pt
刻度数字: 8pt
图例: 8pt
数据来源注释: 6pt, 灰色 (#666666), 图底部
样本量标注: 8pt, 图右上角或图例旁

中文字体: SimHei / Noto Sans SC
英文字体: Arial / Helvetica
数字/代码: JetBrains Mono / Consolas（仅表格数值格使用）
```

### 2.3 线宽与标记

```
坐标轴线: 0.8pt, 黑色 (#000000)
网格线: 0.4pt, 浅灰 (#cccccc), 虚线 (--), 仅水平网格线（纵向网格线禁止）
数据线: 1.5–2.5pt（主要趋势线 2.5pt, 次要线 1.5pt）
数据标记: 5–7pt, 线宽与标记大小匹配
误差线: 1.0pt, 线帽宽度 3pt
```

### 2.4 图表元素规则

```
必须有的元素:
  ├─ 标题（学术结论句——"功率半导体贡献了60%的增幅，高端SoC贡献<5%"好；
  │                      "分品类国产化率对比图"不合格，这是描述性标签）
  ├─ 轴标签（变量名 + 单位）
  ├─ 图例（位置不遮挡数据）
  ├─ 置信区间或误差线（如数据支持）
  ├─ 样本量标注（N=XXXX）
  └─ 数据来源注释（6pt, 灰色, 图底部）

禁止的元素:
  ├─ 3D 效果
  ├─ 纵向网格线
  ├─ 装饰性背景色或渐变
  ├─ 图表边框（spines 只保留左和下，或全部去掉）
  ├─ 冗余标题重复（图标题讲"这张图证明了什么"，正文讲"这意味着什么"）
  └─ 过度密集的刻度标签（轴刻度 ≤ 6 个标签）
```

### 2.5 尺寸与导出

```
尺寸: 宽度 6–8 inches, 高度按数据复杂度自动计算（宽高比 1.3–1.6）
格式: SVG（矢量，文字可编辑，可直接嵌入 HTML）
DPI: ≥ 300（导出 PNG 时）
背景: 透明（SVG）/ 白色（PNG）
```

---

## 三、表格 SVG 渲染规范

所有正文中的表格用 Python 手动渲染为 SVG（`render_tables.py` 或等价脚本），不使用 HTML `<table>`。

### 3.1 表格样式

```
表头: 深色背景 #0b1120 + 白色文字 + 10.5pt + bold
表体:
  ├─ 奇数行: 白色 #ffffff
  ├─ 偶数行: 浅灰 #fafbfc
  └─ 行间分隔线: 0.5pt, #f0f0f0
表头下划线: 2pt, #0b1120
表底收口线: 2pt, #0b1120
内边距: 水平 12px, 垂直 7px
字号: 表头 10.5pt, 表体 10pt
```

### 3.2 表格文字对齐

```
文字列: 左对齐
数值列: 右对齐 + 等宽数字 (tabular-nums) + 次级文本色 (#5a6170)
评分列: 居中 + 粗体 + 紫色高亮 (#553c9a)
关键数字列（如绿色增长率）: 右对齐 + 粗体 + 绿色 (#276749)
```

### 3.3 表格文字溢出处理

```
列宽上限: 420px
超宽文字: 自动换行（wrap），行高 18px
中文字符宽度: ≈ 1.0 × 字号
ASCII 数字宽度: ≈ 0.55 × 字号
ASCII 字母宽度: ≈ 0.58 × 字号
```

---

## 四、图件数量与类型配置原则

图件总数控制在 6–10 张（含表格）：

- 最少：6 张（少于 6 张说明可视化维度不够）
- 最多：10 张（超过 10 张读者视觉疲劳）
- 图表（matplotlib 生成）vs 表格（脚本生成）比例推荐 1:1 到 2:1

至少包含：
- 1 张多维对比图（雷达图/分组柱状图/热力图，展示"谁好谁差"）
- 1 张结构分化图（分组折线图/分组柱状图，展示"总体数字掩盖的内部断裂"）
- 1 张时间线或演化图（折线图/气泡图/时间轴，展示"变化趋势"）

---

## 五、执行流程

1. **提取数据→映射图表类型**：从 `research_data.md` 提取每个需要可视化的数据锚点，按第一章决策树选类型，输出"数据→图表类型"映射表。
2. **并行生成图表**：每张图执行完整绘图合约（结论→证据链→面板→配色→导出）。图 ≥ 6 张时用 Agent 并行。
3. **风格收束**：所有图生成完毕后，统一检查配色/字体/线宽/标记大小。不统一的用脚本批量修正 SVG 样式。
4. **表格渲染**：执行表格脚本，将评分矩阵和关键数据表渲染为 SVG。
5. **更新正文引用**：将正文 Markdown 表格替换为 `<figure><img src="figures/table_NN.svg">` 引用；图表同样处理。`fig_` 前缀为图表，`table_` 前缀为表格。

---

## 质量自检（质量卡口）

**任何一条不通过，留在 Step 5 修改，禁止进入 Step 6。**

- [ ] 每张图的标题为学术结论句（不是描述性标签）
- [ ] 图表类型与数据结构匹配（对照决策树验证）
- [ ] 色盲友好 + 灰阶可区分
- [ ] 无 3D / 无纵向网格线 / 无装饰性背景 / 无饼图
- [ ] 每张图含数据来源注释（6pt, 底部）
- [ ] 所有图的配色/字体/线宽/标记大小一致
- [ ] 表格深色表头 + 交替行 + 紧凑间距
- [ ] 数值列右对齐 + 评分列居中
- [ ] SVG 文字可编辑（svg.fonttype='none'）
- [ ] 图件总数 6–10 张，至少 1 张多维对比 + 1 张结构分化 + 1 张时间线
- [ ] **不包含任何"这张图对应报告哪一节""这个表放在文章哪个位置"的内容——那些属于 Wcget-outline**
