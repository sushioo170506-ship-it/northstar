---
description: Wcget Step 1 大纲 Skill — 五步构建法（定刺→拆骨→填肉→设门→埋钩），产出可争论的大纲而非话题列表。主编排器进入 Step 1 时读取本文件。
globs:
alwaysApply: false
---

# Wcget-outline: 五步构建大纲

**大纲不是话题列表。大纲是一个可争论的结论 + 为这个结论辩护的证据链 + 读者可能质疑点的预判回应。**

## 输入

用户输入的一句话主题。

## 输出

结构化大纲。用自然语言写在对话中，并建议同步写入 `research-workflow/output/outline.md`（供 Step 2 引用）。结构见文末「输出格式」，模板：`research-workflow/templates/outline.template.md`。

> 用户可指定章节骨架（如「背景→介绍→横向对比→局限风险→战略启示」）。此时：**保留用户的章节命名与顺序**，但每节仍强制满足「一句话章节结论 + ≥1 锚点」，不得退化成话题列表。

---

## 五步构建法（严格顺序执行）

### 第一步：定「刺」——找到那个让人不舒服的结论

**先写结论，不要先列资料。** 研究性文章的大纲必须从一个可争论的结论开始，而不是从一个话题开始。写不出一个会让人皱眉的结论，说明还没开始思考。

**操作**：用一句话写出核心结论，必须同时满足三条：

| 条件 | 检验方法 |
|------|---------|
| **可争论** | 有没有人会反对？所有人都同意则没有信息量 |
| **可证伪** | 什么数据出现后结论会变错？没有任何数据能推翻它，它是信仰不是结论 |
| **可行动** | 读者读完知道该做什么不同决定吗？不知道则没有读者价值 |

**格式**：

```
核心结论: [一句话，≤50 字]
为什么有人会不同意: [反对者会怎么说]
在什么条件下这个结论是错的: [证伪条件]
```

**禁止**：描述性结论（「XX 产业发展迅速」「XX 值得关注」）——这是新闻标题，不是结论。

---

### 第二步：拆「骨」——用「证伪思维」搭章节

专业读者潜意识在问四个问题。章节结构应主动回应，而非罗列信息：

| 读者在问 | 对应章节 | 不是 |
|---------|---------|------|
| 「这个结论站得住吗？有什么证据？」 | §1 全景判断（总体数字 + 拆开看结构） | 不是「行业背景」 |
| 「谁好谁差？差在哪？拍脑袋还是有评分体系？」 | §2 深度评比（多维评分 + 实景评价） | 不是「技术介绍」 |
| 「所以谁会赢？什么时候？预测可靠吗？」 | §3 格局推演（分赛道预测 + 时间窗口） | 不是「趋势展望」 |
| 「我是 X 角色，告诉我该做什么。」 | §4 决策工具（Checklist / 评分卡） | 不是「建议」 |

**操作**：每段写一个「章节结论」（该节核心判断），而非章节标题：

```
§N 章节结论: [一句话判断]
  读者质疑: …
  回应方式: [结构分化 / 公开评分 / 情景+触发条件 / 决策表]
  锚点数据: [1–2 个关键数字或待采集指标]
```

> 用户自定义骨架时：把上述「四问逻辑」映射到用户章节上（例如「模型介绍」承接§1的结构分化职责，「横向对比」承接§2评比职责），保证每节仍在为核心结论辩护。

---

### 第三步：填「肉」——每个章节必须有一个「锚点」

章节不能只有标题。每节至少一个具体锚点作为写作支点。没有锚点的章节会写成泛泛而谈。

| 锚点类型 | 定义 | 示例 |
|---------|------|------|
| **数据锚** | 精确量化、有反常识属性的数字 | 「标称 X，独立评测仅 Y」 |
| **案例锚** | 可追溯到真实事件的微型叙事 | 「某版本发布后社区出现的具体故障/突破」 |
| **对比锚** | 两个同类对象的直接对比 | 「A vs B：同任务延迟 / 成本 / 错误率」 |

**操作**：每节标注锚点类型与内容；数字未采集时写清「待 Step 2 采集的指标名」。某节找不到锚点 → 在数据需求清单加重该节，而不是用空话填满。

---

### 第四步：设「门」——控制阅读节奏

**头门（开头 3 秒）**：读者凭什么停下来？三选一：
- A. 反直觉数字
- B. 认知冲突（「我以为 A，数据显示 B」）
- C. 具体场景（「这说的不就是我吗」）

头门内容：15–25 字，含数字（数字待采则占位并进数据需求清单）。

**中间门（每节首段）**：只读每节第一段能否获取该节 80% 信息量？

```
§1 入口段: [≤100 字，含章节结论 + 锚点]
§2 入口段: …
```

**尾门（结尾闭环）**：三选一——回扣标题 / 开放问题 / 行动指令。20–30 字。

**禁令**：结尾不准用「未来可期」「前景广阔」「拭目以待」——这些词的意思是「我不知道」。

---

### 第五步：埋「钩」——传播节奏在大纲阶段定型

```
[3 秒入口]      ← 头门。不要背景，不要铺垫。
  ↓
[前 30% 篇幅]   ← 核心判断 + 最强证据。上来就捅刀子。
  ↓
[中 40% 篇幅]   ← 深钻：评分模型 + 实景 + 假说检验。
  ↓
[后 30% 篇幅]   ← 决策工具 + 总结闭环。
  ↓
[闭环末尾]      ← 尾门。回扣开头冲突。
```

**字数预算**（总 3,000–4,000，供 Step 4 遵守）：

```
头门（含标题+引语）:    100–150 字
核心判断段:             300–400 字
深度评比段:           1,200–1,800 字
格局推演段:             400–600 字
决策工具段:             400–600 字
尾门闭环:               150–200 字
```

---

## 输出格式

```markdown
# [主题] — 大纲

## 核心结论（定刺）
- 结论: [一句话]
- 反对者会说: [预测反驳]
- 证伪条件: [什么情况下结论是错的]

## 章节骨架（拆骨）
- §1 章节结论: [一句话] | 锚点: [数据锚/案例锚/对比锚 + 内容]
- §2 章节结论: [一句话] | 锚点: …
- §3 章节结论: [一句话] | 锚点: …
- §4 章节结论: [一句话] | 锚点: …

## 传播节奏（设门+埋钩）
- 头门（3 秒入口）: [15–25 字]
- 中间门（各节入口段）: [每段 ≤100 字]
- 尾门（闭环末尾）: [20–30 字]
- 节奏分配: 前 30% 捅刀子 / 中 40% 深钻 / 后 30% 工具+闭环

## 数据需求清单
- 为每个锚点列出需要采集的数据维度
- 每个维度标注来源类型（官方公告/技术报告/论文/第三方评测/用户社区/财报等）

## 刺点清单
- 该领域 ≥ 3 条行业共识
- 每条对应的刺点判断
- 刺点检验方式（需要什么数据判定谁对谁错）
```

---

## 自检清单（质量卡口）

**任何一条不通过，回到对应步骤修改，禁止进入 Step 2。**

- [ ] **一句话能说完核心观点？** 把「核心结论」念给外人，他会不会追问「所以呢」？会 → 不合格。
- [ ] **每个章节都在为核心结论辩护？** 能否串成「因为 §1+§2，所以 §3，因此 §4」？不能 → 有冗余章节。
- [ ] **有 ≥ 3 个锚点？** 每节至少一个。只有标题没锚点 → 删或补数据需求。
- [ ] **2 分钟能否掌握全文逻辑？** 只读各节章节结论是否自洽？
- [ ] **头门/尾门能单独传播？** 头门能当标题？尾门能当转发语？

通过后在回复中写：

```
Step 1 质量卡口：全部通过
→ 等待用户确认后进入 Step 2（Wcget-data）
```

---

## 禁令

- 禁止「一、背景 二、技术 三、应用 四、展望」式话题列表
- 禁止无证伪条件的信仰型结论
- 禁止无锚点的空章节
- 禁止在本 Step 大规模搜资料写正文（采集属于 Step 2）
- 允许为定刺做极少量事实核对；若刺点完全不成立，应改刺而非硬写
