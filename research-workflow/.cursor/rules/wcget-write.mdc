---
description: Wcget Step 4 正文 Skill — 纯写作方法论：行文风格、语言规范、数据插入规则、段落模式、阅读节奏、字数约束。主编排器进入 Step 4 时读取本文件。
globs:
alwaysApply: false
---

# Wcget-write: 正文撰写方法论

**本 Skill 只做一件事：规范"怎么写"——行文风格、语言规则、数据插入方式、段落模式、节奏控制。** "写什么"（大纲结构、分几章、每章核心判断）由 Wcget-outline 定义，"哪些数据可用"由 Wcget-data 和 Wcget-process 提供。撰写时从 outline 拿骨架、从 research_data 拿数据锚点、从本 Skill 拿写作规则，三者合一开始动笔。

## 可调用工具（可选/可替换）

> 原文引用了 `nature-writing` / `nature-polishing` / `research-report` 等外部 Skill。在 Cursor 环境若无这些工具，可跳过；其"结构级审查 / 逐段润色 / 逻辑链闭环检查"的职责由本 Skill 的语言宪法与质量自检直接承担。

| 工具 | 时机 | 作用 |
|------|------|------|
| 结构级审查（如 `nature-writing`） | 初稿完成后（推荐） | 审查逻辑链、证据支撑、句间过渡 |
| 逐段润色（如 `nature-polishing`） | 定稿前（可选） | 术语精确、句式紧凑、去冗余 |
| 子 Agent 分写 | 刺点 ≥5 个且时间紧（可选） | 每个刺点独立生成初稿再合并；**骨架段必须人工写** |

**调用纪律**：本 Skill 的语言宪法（一、二、三章）具有最终否决权——任何润色建议与本 Skill 冲突时，以本 Skill 为准。

## 输入

- `research-workflow/output/research_data.md`（评分矩阵 + 交叉验证 + 实景数据）
- Step 1 大纲（章节骨架、刺点预埋、锚点分布、传播节奏）

## 输出

`research-workflow/output/report.md`（2500–4000 字）

---

## 一、语言宪法

### 1.1 禁止词汇（出现即删除或替换）

| 禁止 | 原因 | 替换为 |
|------|------|--------|
| "随着……的发展" | 零信息量的万能开头 | 直接给时间戳 |
| "在当今世界""近年来" | 没有告诉读者任何具体信息 | 给出具体年份或数据 |
| "有望""未来可期""前景广阔" | 无判断套话，掩盖"我不知道" | 给出可检验的预测，或删除 |
| "取得突破""进展显著" | 除非后面跟具体数字 | 给出数字，或保留数字去掉套话 |
| "惊人""戏剧性""不可思议""疯狂""史诗级""划时代""颠覆式" | 形容词替数据做结论 | 删除形容词，让数据本身产生冲击力 |
| "卡脖子" | 笼统，不精确 | 精确描述壁垒类型："制程依赖""认证周期约束：X 年""IP 授权壁垒""设备禁运" |
| "弯道超车""换道超车" | 口号，不是分析 | "技术路线替代""后发优势的具体体现：[数据]" |
| "风口""赛道" | 投资黑话，不精确 | "产业方向""细分市场""增长区间" |
| "吃肉""喝汤""收割" | 商业黑话 | "获取主要利润（毛利率 X%）""占据高价值环节""市场份额集中于前三名（合计 Y%）" |

### 1.2 优先句式

| 场景 | 句式 |
|------|------|
| 给出发现 | "数据显示……但关键不在 X，在 Y。" |
| 挑战共识 | "与行业共识不同，我们发现……" |
| 给出含义 | "这意味着……" |
| 给出反证 | "但以下证据不支持这一判断：……" |
| 给出局限 | "需要指出，本文的分析至少在以下方面存在局限：……特定条件是 [X]，在该条件下以上判断可能不成立。" |
| 给出行动含义 | "如果你的角色是 [X]，以上判断对你的含义是：[一行可操作的动作]。" |

### 1.3 因果推断用词纪律

因果关系的用词必须与证据强度严格匹配。错配是最常见的学术写作硬伤。

| 证据强度 | 可用表述 | 禁止表述 |
|---------|---------|---------|
| 随机实验或准实验设计（DID/RDD/IV） | "导致""使得""引起" | — |
| 多元回归控制可观测变量 | "与……显著相关（p < X）""解释了 X% 的变异" | "导致""使得" |
| 仅为时间序列趋势 | "呈上升/下降趋势""与……同步变化" | "由于""因为""引起" |
| 仅为截面相关性 | "与……正/负相关（r = X）""伴随……" | 任何因果性表述 |
| 仅有产业报告数据（无实验/回归） | "数据显示 A 和 B 同时发生""A 的增长与 B 的增长在时间上重叠" | "由于""因为""A 推动了 B" |

---

## 二、数据插入规则

### 2.1 数据三要素

正文中每个关键数字首次出现时，必须完整包含以下要素。缺失任一则该句需要重写。

```
要素 1: 数字本身（绝对值 + 百分比 + 基数）
要素 2: 时间戳（哪一年/哪个季度/截止日期）
要素 3: 来源标注（机构 + 年份，括号内。如 (中汽协, 2026)）
```

**合格示例**：
`"2026 年 H1，中国新能源乘用车单车平均搭载芯片 1,247 颗（中汽协，2026），较 2023 年的 986 颗增长 26.5%。"`

**不合格示例**：
`"单车芯片数量快速增长。"` / `"国产化率大约在 10–15% 之间。"` / `"业内人士认为国产芯片进步明显。"`

### 2.2 数据锚点插入位置

大纲中标注的每个锚点（数据锚/案例锚/对比锚）在正文中对应位置插入时必须：

1. **锚点数据出现在段首前三句以内**——读者不应该在中段才找到段首判断的数据支撑
2. **锚点后紧跟一句"这意味着"型解读**——数据不能裸奔
3. **数据需要参照系**：孤立数字没有意义。标注同比/环比/与行业均值的差距/与国际标杆的差距

```
数据插入三句式：
[主题句 — 本段的判断]
[数据句 — 具体数字 + 参照系 + 来源标注]
[解读句 — "这意味着……" / "这个差距说明……"]
```

**对照示例**：
```
差：
"地平线 J6 的算力为 560 TOPS。"

好：
"地平线 J6 标称算力 560 TOPS（地平线招股书, 2025），纸面上已超过 NVIDIA Orin-X 的 254 TOPS。
但这颗芯片尚未量产——其真实场景评分为 2.0†（无量产记录）。
这意味着纸面算力对标 ≠ 商业竞争力对标：OEM 主力车型的智驾芯片切换至少需要一代车型的完整验证周期。"
```

### 2.3 评分数据的正文引用

从 `research_data.md §评分矩阵` 引用评分时：

```
--直接引用分数--
"在能效维度（TOPS/W），华为 MDC 810 得分 8.7 分（满分 10.0），国产最高。
10 分锚点为 NVIDIA Thor 的 6.5 TOPS/W（NVIDIA GTC 2025），
5 分锚点为行业均值 2.5 TOPS/W（TrendForce, 2025）——
华为的 4.2 TOPS/W 位于锚点区间的 68% 位置。"

--对比两个产品--
"NVIDIA Orin-X 综合 8.1 vs 华为 MDC 810 综合 7.0。
差距的 60% 来自软件生态维度（NVIDIA 10.0 vs 华为 6.0）——这是 CUDA 300 万开发者 vs CANN <2 万开发者的生态鸿沟。"
```

---

## 三、段落与句式规则

### 3.1 一段一职

每段执行且仅执行一个职能。段首第一句声明该职能。

| 职能 | 段首句式 | 适用场景 |
|------|---------|---------|
| 判断 | "本文的核心发现是：……" / "判断 X：……" | 给出核心结论时 |
| 发现 | "数据显示，……" / "基于 N=XXXX 的样本，……" | 呈现数据支撑时 |
| 对比 | "与该发现形成对照的是，……" | 两个数据点冲突时 |
| 机制 | "驱动这一模式的机制可能包括：……" | 解释因果关系时 |
| 异质性 | "上述结论在 [维度] 中表现出显著异质性：……" | 分组对比结果时 |
| 假说排除 | "另一种可能的解释是 [假说]。但以下证据不支撑这一解读：……" | 排除竞争性假说时 |
| 局限 | "需要指出，本文的分析至少在以下方面存在局限：……" | 声明研究局限时 |
| 含义 | "上述发现的 [行业/投资/政策] 含义在于：……" | 从数据推导行动时 |

### 3.2 句式长度控制

| 句式类型 | 字数上限 | 使用频率 |
|---------|---------|---------|
| **核心判断句**（独立成行） | ≤ 25 字 | 每节 1–2 句 |
| **常规分析句** | 20–50 字 | 占比约 70% |
| **方法/数据描述句** | ≤ 60 字 | 每段最多 2 句 |
| **纯过渡句** | 0 字 | **禁止**——任何纯过渡句必须与数据句合并，或删除 |

**句式节奏规则**：连续长句（>35 字）不超过 3 个。连续短句（<15 字）后用中等句（20–30 字）缓冲。读者需要呼吸。

### 3.3 段落长度

4–8 句，不超过 200 字。超过则拆为两段——每段必须有独立职能，不能"第一段的延续"。

---

## 四、刺点段落模板

刺点是大纲中预埋的反共识判断。正文中的每个刺点按以下五段式结构写。这是本 Skill 最重要的段落模板。

```
[共识]：业内普遍认为 [行业共识的具体表述]（来源：谁在什么场合说了什么 + 时间）。

[证据支撑共识]：[如果共识有数据支撑，先列出——显得客观公正]（来源 + 时间）。

[刺点]：但以下数据不支持这一判断：[数据 1 + 来源]，[数据 2 + 来源]。
  这两个数字与共识的预期方向不一致——[量化说明怎么不一致]。

[修正判断]：因此，更准确的判断是 [修正后的结论]。

[边界]：在 [特定条件] 下，共识可能成立——[什么时候共识是对的]。
  但在 [当前条件] 下，刺点更接近事实——[什么时候刺点是对的]。
  当前最接近 [哪个条件]，因此本文的判断偏向 [共识/刺点]。
```

**刺点写作纪律**：
- 先列出支持共识的证据——"看似公正"是刺点可信度的前提
- 刺点必须有至少两个独立数据点支撑
- 边界条件必须写——"什么情况下我是错的"让刺点更可信，而非更弱
- 刺点的字数控制在 150–250 字

---

## 五、阅读节奏控制

### 5.1 开篇规则

**禁止**以以下任何方式开篇：
- "随着……的发展" / "在当今世界" / "XX 是一个重要的产业"
- 字典式定义
- 背景铺垫（"XX 行业自 19XX 年以来经历了三个阶段……"）

**开篇必须**：
- 第一句就是一个刺点/反常识数字/认知冲突
- 第二句紧跟数据来源标注
- 第三句给出"这意味着"

### 5.2 跳读友好规则

1. **每节首段为该节摘要**：只读所有节的首段 ≈ 全文 80% 信息量
2. **关键数字加粗**：每个核心数据点用 `**` 包裹
3. **判断句独立成行**：核心判断不嵌入段落中间，必须让扫读的人一眼看到
4. **小标题是判断句**：不用"市场现状"这种描述性标题，用"12% 的国产化率是海市蜃楼"这种判断句

### 5.3 传播节奏映射

大纲中定义的传播节奏（前 30% / 中 40% / 后 30%）在正文中的落地方式：

```
[前 30%] 判断密集区:
  - 句长偏短（平均 20–25 字）
  - 每段 1 个核心数字
  - 段落简短（3–5 句/段）
  - 目的：读者在 30 秒内决定"值不值得继续看"

[中 40%] 证据密集区:
  - 句长正常（平均 25–40 字）
  - 每段 2–3 个支撑数据
  - 数据锚点集中插入
  - 目的：看门道的读者从这里开始，看热闹的可以跳过

[后 30%] 行动密集区:
  - 句长偏短
  - "如果你是 [角色]"句式
  - 每条建议 ≤ 2 句话
  - 闭环金句（≤ 30 字）
  - 目的：读者在这决定"值不值得转发/收藏"
```

---

## 六、字数控制

硬上限 4,000 字。超限时的删除优先级（从先到后）：

1. **删除背景铺垫**：读者不需要被教育行业基础——直接进入判断
2. **删除纯过渡段**：两个主要节之间的"桥接段"如果无数据点，删掉
3. **合并同类数据段**：两段在说同一件事的不同侧面 → 合并为一段
4. **降级边缘发现**：对核心判断支撑最弱的发现挪到脚注或删除

**不可删除的内容**（删这些是破坏文章骨架）：
- 大纲中的核心判断段（§1–§4 的章节结论）
- 刺点段落（每个刺点是独立的传播单元）
- 数据锚点（每个锚点是文章可信度的支柱）
- 研究局限声明
- 闭环金句

---

## 七、引用格式规范

```
公开数据:         "（来源机构, 年份）"
企业年报/招股书:   "（企业名, 年报/招股书, 财年）"
第三方研究报告:     "（机构名, 报告名, 年份）"
学术论文:         "（作者, 期刊/会议缩写, 年份）"
实景评价:         "（来源平台, N=样本量, 年份）"
```

所有引用在文末按出现顺序编号为参考文献列表。

---

## 质量自检（质量卡口）

**任何一条不通过，留在 Step 4 修改，禁止进入 Step 5。**

- [ ] 全文无禁止词汇清单中的任何词汇
- [ ] 每个关键数字首次出现时具备三要素（数字 + 时间 + 来源）
- [ ] 每个数据锚点后紧跟"这意味着"型解读句
- [ ] 每段段首声明职能（判断/发现/对比/机制/假说排除/局限/含义）
- [ ] 核心判断句 ≤ 25 字，独立成行
- [ ] 刺点段落按五段式模板写：[共识]→[证据]→[刺点]→[修正判断]→[边界]
- [ ] 开篇第一句是反常识数字/认知冲突（不是背景介绍）
- [ ] 每节首段可独立阅读（为该节摘要）
- [ ] 因果推断用词与证据强度匹配
- [ ] 无纯过渡段
- [ ] 字数 2,500–4,000
- [ ] **不包含任何决定"文章应该分几章""每章应该写什么判断""评分模型应该放哪一节"的内容——那些属于 Wcget-outline 和 Wcget-process**
